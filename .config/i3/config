# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

bindsym $mod+$mod2+o exec alacritty -e pulsemixer, exec easyeffects, exec alacritty --title terminal
bindsym $mod+$mod3+o exec wm o
bindsym $mod+o exec wm m

# lock
bindsym $mod+$mod3+l exec i3lock -c 000000

# suspend
bindsym $mod+$mod3+s exec systemctl suspend

bindsym $mod+$mod2+$mod3+a exec setxkbmap -device $(xinput list --id-only 'AT Translated Set 2 keyboard') -option ctrl:swapcaps -option altwin:swap_lalt_lwin dvorak

bindsym $mod+$mod2+m exec i3-input -F 'mark %s' -l 1 -P 'Mark: '
bindsym $mod+m exec i3-input -F '[con_mark="%s"] focus' -l 1 -P 'Goto: '
bindsym $mod+$mod3+m exec i3-input -F 'exec wm mo %s' -P 'mousesens: '

bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +2% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -2% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status
bindsym XF86AudioNext exec --no-startup-id playerctl next
bindsym XF86AudioPrev exec --no-startup-id playerctl previous
bindsym XF86AudioStop exec --no-startup-id playerctl play-pause

bindsym $mod+semicolon exec i3-input -F "exec i3-msg %s" -P 'i3-msg '

# focus between monitors
bindsym $mod+l focus output right
bindsym $mod+b focus output down

# move between monitors
bindsym $mod+$mod2+l move to output right
bindsym $mod+$mod2+b move to output down

# move workspace
bindsym $mod+$mod2+$mod3+l move workspace to output right
bindsym $mod+$mod2+$mod3+b move workspace to output down

# resize window
bindsym $mod+Right resize grow right 1 px or 2 ppt
bindsym $mod+$mod2+Right exec i3ext swap right
bindsym $mod+Up resize shrink right 1 px or 2 ppt
bindsym $mod+Left resize grow left 1 px or 2 ppt
bindsym $mod+$mod2+Left exec i3ext swap left
bindsym $mod+Down resize shrink left 1 px or 2 ppt

# resize toplevel containers
bindsym $mod+c exec i3ext center
bindsym $mod+r exec i3ext center right
bindsym $mod+g exec i3ext center left
bindsym $mod+z exec i3-input -F "exec i3ext resize %s" -P 'i3ext resize '
bindsym $mod+$mod2+z exec i3-input -F "exec i3-msg [floating] resize set %s" -P 'i3-msg [floating] resize set '

# bindings for attaching to/detaching from an external monitor
bindsym $mod+$mod2+a nop
bindsym $mod+$mod2+d nop

# change focus
bindsym $mod+comma focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+period focus right

# move focused window
bindsym $mod+$mod2+comma move left
bindsym $mod+$mod2+j move down
bindsym $mod+$mod2+k move up
bindsym $mod+$mod2+period move right

bindsym $mod+d scratchpad show
bindsym $mod+$mod2+d move scratchpad

# Rename current window
bindsym $mod+n exec i3-input -F "title_format %s"

# Rename current workspace
bindsym $mod+$mod2+n exec NUM=$(i3-msg -t get_workspaces | jq -r '.[] | select(.focused==true).name' | cut -d':' -f1) && i3-input -F "rename workspace to $NUM:%s"

# Take screenshot and copy to clipboard
bindsym --release $mod+$mod2+$mod3+s exec --no-startup-id ~/.config/i3/screenshot.sh

# Disable window titlebars always
default_border pixel 1

# Set a gaps of 10 (outer gaps is unset because it's added to this)
gaps inner 10

# class                 border  backgr. text    indicator child_border
client.focused          #f58b54 #f58b54 #ffffff #ffffff   #f58b54

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $mod2 Shift
set $mod3 Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 10

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+grave exec alacritty
bindsym $mod+$mod2+grave [workspace="__focused__"] split vertical; [workspace="__focused__"] layout stacking

# start a placeholder
bindsym $mod+Tab open

# start a browser
bindsym $mod+Escape exec chromium

bindsym $mod+equal focus mode_toggle
bindsym $mod+$mod2+equal floating toggle

# This needs to be an external script because reloading i3 destroys SC2.
#bindsym F3 exec stop

# kill focused window
bindsym $mod+$mod2+w kill

# start dmenu (a program launcher)
bindsym $mod+minus exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+$mod2+s split vertical; layout stacking
bindsym $mod+t layout tabbed
bindsym $mod+$mod2+t split horizontal; layout tabbed

bindsym $mod+h layout splith
bindsym $mod+$mod2+h split horizontal
bindsym $mod+v layout splitv
bindsym $mod+$mod2+v split vertical

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+i focus child

# switch to workspace
bindsym $mod+1 workspace number 1
bindsym $mod+2 workspace number 2
bindsym $mod+3 workspace number 3
bindsym $mod+4 workspace number 4
bindsym $mod+5 workspace number 5
bindsym $mod+6 workspace number 6
bindsym $mod+7 workspace number 7
bindsym $mod+8 workspace number 8
bindsym $mod+9 workspace number 9
bindsym $mod+0 workspace number 0
bindsym $mod+$mod3+1 workspace number 11
bindsym $mod+$mod3+2 workspace number 12
bindsym $mod+$mod3+3 workspace number 13
bindsym $mod+$mod3+4 workspace number 14
bindsym $mod+$mod3+5 workspace number 15
bindsym $mod+$mod3+6 workspace number 16
bindsym $mod+$mod3+7 workspace number 17
bindsym $mod+$mod3+8 workspace number 18
bindsym $mod+$mod3+9 workspace number 19
bindsym $mod+$mod3+0 workspace number 20

# move focused container to workspace
bindsym $mod+$mod2+1 move container to workspace number 1
bindsym $mod+$mod2+2 move container to workspace number 2
bindsym $mod+$mod2+3 move container to workspace number 3
bindsym $mod+$mod2+4 move container to workspace number 4
bindsym $mod+$mod2+5 move container to workspace number 5
bindsym $mod+$mod2+6 move container to workspace number 6
bindsym $mod+$mod2+7 move container to workspace number 7
bindsym $mod+$mod2+8 move container to workspace number 8
bindsym $mod+$mod2+9 move container to workspace number 9
bindsym $mod+$mod2+0 move container to workspace number 0
bindsym $mod+$mod2+$mod3+1 move container to workspace number 11
bindsym $mod+$mod2+$mod3+2 move container to workspace number 12
bindsym $mod+$mod2+$mod3+3 move container to workspace number 13
bindsym $mod+$mod2+$mod3+4 move container to workspace number 14
bindsym $mod+$mod2+$mod3+5 move container to workspace number 15
bindsym $mod+$mod2+$mod3+6 move container to workspace number 16
bindsym $mod+$mod2+$mod3+7 move container to workspace number 17
bindsym $mod+$mod2+$mod3+8 move container to workspace number 18
bindsym $mod+$mod2+$mod3+9 move container to workspace number 19
bindsym $mod+$mod2+$mod3+0 move container to workspace number 20

# reload the configuration file
bindsym $mod+$mod2+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+$mod2+r restart
# exit i3 (logs you out of your X session)

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
	position top
        status_command i3status
	tray_output primary
}
